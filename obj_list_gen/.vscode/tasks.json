{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "build",
			"command": "${env:HOME}/.dotnet/dotnet",
			"type": "process",
			"args": [
				"build",
				"${workspaceFolder}/EsiCodeGen.csproj"
			],
			"problemMatcher": "$msCompile",
			// "envFile": "${workspaceFolder}/.env"
			"options": {
				"env": {
					"DOTNET_ROOT": "${env:HOME}/.dotnet",
					"PATH": "${env:DOTNET_ROOT}:${env:DOTNET_ROOT}/tools:${env:PATH}"
				}
			}
		}
	]
}